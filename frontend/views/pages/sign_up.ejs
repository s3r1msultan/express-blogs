<!DOCTYPE html>
<html lang="en">
  <%- include('../blocks/head', {title: "Sign up"}) %>
  <body>
    <%- include('../blocks/header', {isLoggedIn: isLoggedIn, isAdmin: isAdmin, language: language})
    %>
    <main>
      <div class="container">
        <% if (language == "ru") { %>
        <h1 class="fw-bold text-center mt-5">Регистрация</h1>
        <form
          action="/auth/sign_up"
          method="POST"
          class="d-flex flex-column justify-content-center align-items-center"
        >
          <div class="d-flex flex-column align-items-end">
            <label for="username"
              >Имя пользователя:
              <input
                id="username"
                name="username"
                type="text"
                required
            /></label>
            <label
              class="mt-3"
              for="first_name"
              >Имя:
              <input
                id="first_name"
                name="first_name"
                type="text"
                required
            /></label>
            <label
              class="mt-3"
              for="last_name"
              >Фамилия:
              <input
                id="last_name"
                name="last_name"
                type="text"
                required
            /></label>
            <label
              class="mt-3"
              for="email"
              >Email:
              <input
                id="email"
                name="email"
                type="email"
                required
            /></label>
            <label
              class="mt-3"
              for="password"
              >Пароль:
              <input
                id="password"
                name="password"
                type="password"
                required
            /></label>
          </div>
          <% if (wrong) { %>
          <div class="bg-danger border rounded p-1 mt-3">
            Пользователь с таким именем или email уже существует.
          </div>
          <% } %> <% if (successful) { %>
          <div class="bg-success border rounded p-1 mt-3">
            Мы отправили вам письмо с потверждением. Подтвердите свою учетную запись.
          </div>
          <% } %>
          <div class="d-flex align-items-center">
            <input
              class="mt-3 p-2"
              type="submit"
              value="Зарегистрироваться"
            />
            <a
              class="ms-5 pt-3"
              href="/auth/sign_in"
              >Вход</a
            >
          </div>
        </form>
        <% } else { %>
        <h1 class="fw-bold text-center mt-5">Sign Up</h1>
        <form
          action="/auth/sign_up"
          method="POST"
          class="d-flex flex-column justify-content-center align-items-center"
        >
          <div class="d-flex flex-column align-items-end">
            <label for="username"
              >Username:
              <input
                id="username"
                name="username"
                type="text"
                required
            /></label>
            <label
              class="mt-3"
              for="first_name"
              >First name:
              <input
                id="first_name"
                name="first_name"
                type="text"
                required
            /></label>
            <label
              class="mt-3"
              for="last_name"
              >Last name:
              <input
                id="last_name"
                name="last_name"
                type="text"
                required
            /></label>
            <label
              class="mt-3"
              for="email"
              >Email:
              <input
                id="email"
                name="email"
                type="email"
                required
            /></label>
            <label
              class="mt-3"
              for="password"
              >Password:
              <input
                id="password"
                name="password"
                type="password"
                required
            /></label>
          </div>
          <% if (wrong) { %>
          <div class="bg-danger border rounded p-1 mt-3">
            A user with this username or email already exists.
          </div>
          <% } %> <% if (successful) { %>
          <div class="bg-success border rounded p-1 mt-3">
            We have sent you a confirmation e-mail. Confirm your account.
          </div>
          <% } %>
          <div class="d-flex align-items-center">
            <input
              class="mt-3 p-2"
              type="submit"
              value="Sign Up"
            />
            <a
              class="ms-5 pt-3"
              href="/auth/sign_in"
              >Log In</a
            >
          </div>
        </form>
        <% } %>
      </div>
    </main>

    <%- include('../blocks/footer') %>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
